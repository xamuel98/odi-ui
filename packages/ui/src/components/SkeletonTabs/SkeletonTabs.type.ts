export interface SkeletonTabsProps {
  /**
   * Number of tabs to display.
   * @default 2
   */
  count?: number;
  /**
   * Whether the tabs should fit the container width.
   * @default false
   */
  fitted?: boolean;
}

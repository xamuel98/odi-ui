import { primitiveSpace } from "../primitives/space.js";

export const space = {
  buttonGroupGap: primitiveSpace["200"],
  cardGap: primitiveSpace["400"],
  cardPadding: primitiveSpace["400"],
  tableCellPadding: primitiveSpace["150"],
} as const;
